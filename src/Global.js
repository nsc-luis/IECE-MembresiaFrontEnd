var Global = {
    url_api : "http://" + window.location.hostname + ":59239/api",
    //url_api : "http://" + window.location.hostname + "/webapi/api"
}

export default Global;
// export default validaFormatos;